# Create a execute client.exe

`gcc client.c -o client.exe`

# Create a execute server.exe

`gcc server.c -o server.exe`

# Note

Run server first then run the client
`./server` to run the server
`./client` to run the client
